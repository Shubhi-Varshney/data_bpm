import pandas as pd
import ml-logic.data.load_data_to_bq

def preprocess_features(X: pd.DataFrame):

    # To Do:
    # Preprocess features
    # After preprocessing, load the preprocessed data into bigquery
    load_data_to_bq
